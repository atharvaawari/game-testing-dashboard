<!DOCTYPE html>
<html>

<head>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0">
  <title>My Task</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <!-- <<<<<<<<< - my style sheet - >>>>>>>>>> -->
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Nunito', sans-serif;
      background-color: #f3f4f6;
      margin: 0;
      padding: 0;
      text-align: center;
    }

    a {
      text-decoration: none;
    }

    label {
      font-size: 18px;
      vertical-align: sub;
      font-weight: 700;
      display: block
    }

    .input-box {
      text-align: left;
      margin: 1.5rem 1rem 0px 1rem;
    }

    h1 {
      display: block;
      margin-bottom: 0.5rem;
      color: #555;
      margin-top: 1rem;
      font-weight: 600;
    }

    h2 {
      color: #333;
      margin-top: 2rem;
    }

    h4 {
      text-transform: capitalize;
      text-align: left;
      font-weight: 700;
      font-size: 16px;
    }

    input[type="date"] {
      width: 100px;
      padding: 0.5rem;
      margin-bottom: 1rem;
      border: 1px solid #e0e0e0;
      border-radius: 4px;
    }

    button {
      background-color: #007BFF;
      color: #ffffff;
      border: none;
      border-radius: 4px;
      padding: 0.5rem 1rem;
      margin: 0.5rem;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #0056b3;
    }

    .selected {
      background-color: #ffeb3b;
    }

    small {
      font-size: .875em;
      padding: 6px;
      border-radius: 8px;
      color: gray;
    }



    .w-sm {
      width: 30%;
    }

    .titl2 {
      font-weight: 800;
      font-size: 20px;
      text-transform: capitalize;
    }

    .center {
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: center;
      background: #8a2be233;
      padding: 15px;
      border-radius: 8px;
      height: 100%;
    }

    .tr {
      border: 1px solid black;
      padding: 0 5px;
      background: #F1F6F9;
    }

    .th {
      font-weight: 700;
      border: 1px solid black;
      width: fit-content;
      padding: 10px 14px;
      margin: 30px 0;
      background: #FFF89A;
    }

    .color-F7D716D9 {
      background: #F7D716D9;
    }

    .color-F66B0EE0 {
      background: #F66B0EE0;
    }

    .color-5BF477 {
      background: #5BF477;
    }

    .color-FF5F9E {
      background: #FF5F9E;
    }

    table {
      font-size: 14px;
      caption-side: bottom;
      border-collapse: collapse;
      margin: auto;
      width:100%;
      box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.25) !important;
    }

    .body {
      width: 450px;
      overflow: auto;
      display: none;
      overflow: auto;
      background: white;
      position: fixed;
      left: 51px;
      top: 25px;
      left: 50%;
      transform: translateX(-50%);
      padding: 1rem;
      border-radius: 10px;
      box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.25) !important;
      opacity: 0;
      transition: opacity 0.3s, transform 0.3s;
      z-index: 10000;
    }

    .body.show {
      display: block;
      opacity: 1;
    }

    .th-style {
      padding: .5rem .5rem;
      background-color: #FFD966;
      border-bottom: 1px solid #8080805e;
      font-weight: 800;
    }

    .baged--1 {
      background: #FFCC8F;
      padding: 5px 8px;
      margin: 0 10px;
      border-radius: 7px;
      display: inline-block;
    }

    .category-container {
      display: inline-block;
      min-width: 950px;
    }

    .accordion-item {
      width: 100%;
    }

    .grid-box-7-3 {
      display: grid;
      grid-template-columns: 70% 30%;
    }

    .alert-success {
      z-index: -1 !important;
    }

    .accordion-button button {
      position: absolute;
      right: 5rem;
    }

    .target-details {
      max-width: 30vw;
      height: 50px;
      min-width: 65%;
    }

    .w-btn-input input {
      width: 60px;
      background-color: #fff;
    }

    .w-btn-input {
      display: inline-block;
    }

    .bck-border {
      border: 1px solid black;
      padding: 6px;
      border-radius: 5px;
      background-color: #fff;
      border: 1px solid rgba(0, 0, 0, .125);
    }

    .section-header {
      display: flex;
      justify-content: space-between;
    }

    .accordion-button {
      width: 82%;
    }

    .acc-activity-box {
      display: inline-block;
      padding-top: 6px;
    }

    .accordion-header {
      display: flex;
      justify-content: space-between;
      box-shadow: inset 0 -1px 0 rgba(0, 0, 0, .125);
    }

    .accordion-button::after {
      margin-bottom: 9px;
      margin-left: 10px;
    }

    .accordion-button:not(.collapsed) {
      color: #0c63e4;
      background-color: white;
      box-shadow: inset 0 -1px 0 rgba(0, 0, 0, .125);
    }

    .f-icon {
      font-size: 15px;
      padding: .2rem
    }

    .divider-line {
      box-shadow: inset 0 -1px 0 rgba(0, 0, 0, .125);
    }

    /*  Dropdown */

    .dropdown {
      position: relative;
      display: inline-block;
    }

    .dropdown button {
      padding: 10px;
      font-size: 16px;
      margin-left: 0;
    }

    .dropdown-content {
      display: none;
      position: absolute;
      background-color: #f9f9f9;
      min-width: 200px;
      box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 0.2);
      z-index: 1;
      height: 300px;
      overflow-y: scroll;
    }

    .dropdown-content.show {
      display: block;

    }

    .dropdown-content a {
      padding: 12px 16px;
      display: block;
      text-decoration: none;
      color: #333;
    }

    .dropdown-content a:hover {
      background-color: #ddd;
    }
  </style>

</head>

<body>

  <%- include('../partials/header.ejs') -%>

  <main>

  <div class="category-container mt-3">

  </div>
    
  <section>

    <div class="category-container mb-3">
      <div class="accordion-item">
        <div class="accordion-header" id="">
          <a class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#w--targets-box" aria-expanded="true" aria-controls="collapseThree">
            <h4>This Week Targets</h4>
          </a>
          <div class="acc-activity-box">
          <button class="btn btn-outline-dark ms-5">
            <% let filteredItems1 = getWeeklyTargets.filter(item => item.is_complete == 0);%>
            <%=filteredItems1.length%>
          </button>
          </div>
        </div>
        <div id="w--targets-box" class="accordion-collapse collapse" aria-labelledby="" data-bs-parent="#accordionExample">
          <div class="accordion-body">
            <% if(getWeeklyTargets.length > 0 ) {%>
            <table id='weekly-targets-table'>
              <thead>
                <tr class="th-style">
                  <th class="th-style">Priority </th>
                  <th class="th-style">Title</th>
                  <th class="th-style">Task Details</th>
                  <th class="th-style">Check</th>
                  <th class="th-style"></th>
                </tr>
              </thead>
              <tbody>

                <% let firstIteration = 0; for( a = 0; a < getWeeklyTargets.length; a++) { %>

                <% if(getWeeklyTargets[a].is_complete == 0 ) {%>

                <tr>
                  <td><%=getWeeklyTargets[a].priority %></td>                 
                  <td class="target-details"><%=getWeeklyTargets[a].task_details%></td>
                  <td>
                    <%- include("../task/details.ejs", { dynamicCategory: "weekly" , dynamicValues: getWeeklyTargets[a].more_details , taskId:getWeeklyTargets[a].id}) -%>
                  </td>
                  <td>
                    <% if(getWeeklyTargets[a].is_complete == 1) {%>
                    <input type="checkbox" id="<%=getWeeklyTargets[a].id%>" onclick="handleCheckboxClick(this,'weekly')" checked>
                    <% } else {%>
                    <input type="checkbox" id="<%=getWeeklyTargets[a].id%>" onclick="handleCheckboxClick(this,'weekly')">
                    <% } %>
                  </td>
                  <td ><a href="/tasks-<%=getWeeklyTargets[a].category_name%>"><span class="badge bg-dark"><%=getWeeklyTargets[a].category_name%></span></a></td>
                </tr>

                <% } else { %>

                <% if(firstIteration == 0) {%>

                <tr class="divider-line">
                  <td>
                  
                  </td>
                </tr>

                <%  firstIteration = 1 } %>

                <tr>
                  <td><%=getWeeklyTargets[a].priority %></td>
                  
                  <td class="target-details"><%=getWeeklyTargets[a].task_details%></td>
                  <td>
                    <%- include("../task/details.ejs", { dynamicCategory: "weekly" , dynamicValues: getWeeklyTargets[a].more_details , taskId:getWeeklyTargets[a].id }) -%>
                  </td>
                  <td>
                    <% if(getWeeklyTargets[a].is_complete == 1) {%>
                    <input type="checkbox" id="<%=getWeeklyTargets[a].id%>" onclick="handleCheckboxClick(this,'weekly')" checked>
                    <% } else {%>
                    <input type="checkbox" id="<%=getWeeklyTargets[a].id%>" onclick="handleCheckboxClick(this,'weekly')">
                    <% } %>
                  </td>
                  <td ><a href="/tasks-<%=getWeeklyTargets[a].category_name%>"><span class="badge bg-dark"><%=getWeeklyTargets[a].category_name%></span></a></td>
                </tr>

                <% } %>
                <% } %>

              </tbody>
            </table>
            <% } else { %>
            <table>
              <strong class="py-3">No Data Found </strong>
            </table>
            <% } %>

          </div>
        </div>
    </div>

  </section>

  <section>

    <div class="category-container mb-3">
      <div class="accordion-item">
        <div class="accordion-header" id="">
          <a class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#ew--targets-box" aria-expanded="true" aria-controls="collapseThree">
            <h4>Every Week Targets</h4>
          </a>
          <div class="acc-activity-box">
            <button class="btn btn-outline-dark ms-5">
            <% let filteredItems2 = everyWeekTargets.filter(item => item.is_complete == 0);%>
            <%=filteredItems2.length%>
            </button>
          </div>
        </div>
        <div id="ew--targets-box" class="accordion-collapse collapse" aria-labelledby="" data-bs-parent="#accordionExample">
          <div class="accordion-body">

            <% if(everyWeekTargets.length > 0 ) { %>
            <table id='every-week-targets-table'>
              <thead>
                <tr class="th-style">
                  <th class="th-style">Priority </th>
                  <th class="th-style">Title</th>
                  <th class="th-style">Task Details</th>
                  <th class="th-style">Check</th>
                  <th class="th-style"></th>
                </tr>
              </thead>
              <tbody>

                <% let firstIteration = 0;  for( a = 0; a < everyWeekTargets.length; a++) { %>

                <% if(everyWeekTargets[a].is_complete == 0 ) {%>

                <tr>
                  <td><%=everyWeekTargets[a].priority %></td>
                 
                  <td class="target-details"><%=everyWeekTargets[a].task_details%></td>
                  <td>
                    <%- include("../task/details.ejs", { dynamicCategory: "every-week" , dynamicValues: everyWeekTargets[a].more_details , taskId:everyWeekTargets[a].id }) -%>
                  </td>
                  <td>
                    <% if(everyWeekTargets[a].is_complete == 1) {%>
                    <input type="checkbox" id="<%=everyWeekTargets[a].id%>" onclick="handleCheckboxClick(this,'every-week')" checked>
                    <% } else {%>
                    <input type="checkbox" id="<%=everyWeekTargets[a].id%>" onclick="handleCheckboxClick(this,'every-week')">
                    <% } %>
                  </td>
                  <td ><a href="/tasks-<%=everyWeekTargets[a].category_name%>"><span class="badge bg-dark"><%=everyWeekTargets[a].category_name%></span></a></td>
                </tr>

                <% } else { %>

                <% if(firstIteration == 0) {%>

                <tr class="divider-line">
                  <td>
                  
                  </td>
                </tr>

                <%  firstIteration = 1 } %>
                
                <tr>
                  <td><%=everyWeekTargets[a].priority %></td>
                 
                  <td class="target-details"><%=everyWeekTargets[a].task_details%></td>
                  <td>
                    <%- include("../task/details.ejs", { dynamicCategory: "every-week" , dynamicValues: everyWeekTargets[a].more_details , taskId:everyWeekTargets[a].id }) -%>
                  </td>
                  <td>
                    <% if(everyWeekTargets[a].is_complete == 1) {%>
                    <input type="checkbox" id="<%=everyWeekTargets[a].id%>" onclick="handleCheckboxClick(this,'every-week')" checked>
                    <% } else {%>
                    <input type="checkbox" id="<%=everyWeekTargets[a].id%>" onclick="handleCheckboxClick(this,'every-week')">
                    <% } %>
                  </td>
                  <td ><a href="/tasks-<%=everyWeekTargets[a].category_name%>"><span class="badge bg-dark"><%=everyWeekTargets[a].category_name%></span></a></td>

                </tr>
                <% } %>
                <% } %>

              </tbody>
            </table>
            <% } else { %>
            <table>
              <strong class="py-3">No Data Found </strong>
            </table>
            <% } %>

          </div>
        </div>
    </div>

  </section>

  <section>

    <div class="category-container mb-3">
      <div class="accordion-item">
        <div class="accordion-header">
          <a class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#m--targets-box" aria-expanded="true" aria-controls="collapseThree">
            <h4>This Month Targets</h4>
          </a>

          <div class="acc-activity-box">
          <button class="btn btn-outline-dark ms-5">
          <% let filteredItems3 = monthlyTargets.filter(item => item.is_complete == 0);%>
          <%=filteredItems3.length%>
          </button>
          </div>
        </div>
        <div id="m--targets-box" class="accordion-collapse collapse" aria-labelledby="" data-bs-parent="#accordionExample">
          <div class="accordion-body">

            <% if(monthlyTargets.length > 0 ) {%>
            <table id='monthly-targets-table'>
              <thead>
                <tr class="th-style">
                  <th class="th-style">Priority </th>
                  <th class="th-style">Title</th>
                  <th class="th-style">Task Details</th>
                  <th class="th-style">Check</th>
                  <th class="th-style"></th>
                 
                </tr>
              </thead>
              <tbody>
     
                <% let firstIteration = 0;  for( a = 0; a < monthlyTargets.length; a++) { %>

                <% if(monthlyTargets[a].is_complete == 0 ) {%>
                
                <tr>
                  <td><%=monthlyTargets[a].priority %></td>
                  
                  <td class="target-details"><%=monthlyTargets[a].task_details%></td>
                  <td>
                    <%- include("../task/details.ejs", { dynamicCategory: "monthly" , dynamicValues: monthlyTargets[a].more_details , taskId:monthlyTargets[a].id }) -%>
                  </td>
                  <td>
                    <% if(monthlyTargets[a].is_complete == 1) {%>
                    <input type="checkbox" id="<%=monthlyTargets[a].id%>" onclick="handleCheckboxClick(this,'monthly')" checked>
                    <% } else {%>
                    <input type="checkbox" id="<%=monthlyTargets[a].id%>" onclick="handleCheckboxClick(this,'monthly')">
                    <% } %>
                  </td>
                  <td><a href="/tasks-<%=monthlyTargets[a].category_name%>"><span class="badge bg-dark"><%=monthlyTargets[a].category_name%></span></a></td>

                </tr>

                <% } else { %>

                <% if(firstIteration == 0) {%>

                <tr class="divider-line">
                  <td>
                  
                  </td>
                </tr>

                <%  firstIteration = 1 } %>
             
                <tr>
                  <td><%=monthlyTargets[a].priority%></td>
                  
                  <td class="target-details"><%=monthlyTargets[a].task_details%></td>
                  <td>
                    <%- include("../task/details.ejs", { dynamicCategory: "monthly" , dynamicValues: monthlyTargets[a].more_details , taskId:monthlyTargets[a].id }) -%>
                  </td>
                  <td>
                    <% if(monthlyTargets[a].is_complete == 1) {%>
                    <input type="checkbox" id="<%=monthlyTargets[a].id%>" onclick="handleCheckboxClick(this,'monthly')" checked>
                    <% } else {%>
                    <input type="checkbox" id="<%=monthlyTargets[a].id%>" onclick="handleCheckboxClick(this,'monthly')">
                    <% } %>
                  </td>
                  <td><a href="/tasks-<%=monthlyTargets[a].category_name%>"><span class="badge bg-dark"><%=monthlyTargets[a].category_name%></span></a></td>
                </tr>

                <%  } %>

                <% } %>

              </tbody>
            </table>
            <% } else { %>
            <table>
              <strong class="py-3">No Data Found </strong>
            </table>
            <% } %>

          </div>
        </div>
    </div>

  </section>

  <% if(releaseGames.length > 0 ) {%>
  
      <section>

        <div class="category-container mb-3">
          <div class="accordion-item">
            <div class="accordion-header" id="">
              <a class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#task-box-<%=releaseGames[0].id %>" aria-expanded="true" aria-controls="collapseThree">
                <h4> All Task</h4>
              </a>
              <div class="acc-activity-box">
          <button class="btn btn-outline-dark ms-5">
           <% let filteredItems4 = releaseGames.filter(item => item.is_complete == 0);%>
           <%=filteredItems4.length%>
          </button>
              </div>
            </div>
            <div id="task-box-<%=releaseGames[0].id %>" class="accordion-collapse collapse" aria-labelledby="" data-bs-parent="#accordionExample">
              <div class="accordion-body">
             
                <table id='game-targets-table'>

                  <thead>
                    <tr class="th-style">
                      <th class="th-style">Priority </th>
                      <th class="th-style">Task Owner</th>
                      <th class="th-style">Title</th>
                      <th class="th-style"> Task Details</th>
                      <th class="th-style">Check</th>
                      <th class="th-style"></th>
                    
                    </tr>
                  </thead>

                  <tbody>

                    <% let firstIteration = 0; for( a = 0; a < releaseGames.length; a++) { %>

                    <% if(releaseGames[a].is_complete == 0 ) {%>

                        <tr>
                            <td><%=releaseGames[a].priority %></td>
                            <td><%=releaseGames[a].task_owner %></td>
                            <td class="target-details"><%=releaseGames[a].task_details%></td>
                            <td>
                              <%- include("../task/details.ejs", { dynamicCategory: "game" , dynamicValues: releaseGames[a].more_details , taskId:releaseGames[a].id }) -%>
                            </td>
                            <td>
                              <% if(releaseGames[a].is_complete == 1) {%>
                              <input type="checkbox" id="<%=releaseGames[a].id%>" onclick="handleCheckboxClick(this,'game')" checked>
                              <% } else {%>
                              <input type="checkbox" id="<%=releaseGames[a].id%>" onclick="handleCheckboxClick(this,'game')">
                              <% } %>
                            </td>

                          </tr>
      
                          <% } else { %>
      
                          <% if(firstIteration == 0) {%>
      
                          <tr class="divider-line">
                            <td>
      
                            </td>
                          </tr>
      
                          <%  firstIteration = 1 } %>
      
                          <tr>
                            <td><%=releaseGames[a].priority %></td>
                            <td><%=releaseGames[a].task_owner %></td>
                            <td class="target-details"><%=releaseGames[a].task_details%></td>
                            <td>
                              <%- include("../task/details.ejs", { dynamicCategory: "game"  , dynamicValues: releaseGames[a].more_details , taskId:releaseGames[a].id }) -%>
                            </td>
                            <td>
                              <% if(releaseGames[a].is_complete == 1) {%>
                              <input type="checkbox" id="<%=releaseGames[a].id%>" onclick="handleCheckboxClick(this,'game')" checked>
                              <% } else {%>
                              <input type="checkbox" id="<%=releaseGames[a].id%>" onclick="handleCheckboxClick(this,'game')">
                              <% } %>
                            </td>
                          </tr>

                    <% } %>
                    <% } %>

                  </tbody>

                </table>

              </div>
            </div>
          </div>
        </div>

      </section>

    <% } %>

  </main>
<script>

  function toggleBody(emailId) {
    const body = document.getElementById('body' + emailId);
    body.classList.toggle('show');
  };

  function handleCheckboxClick(elementRef, targetsCategory) {

    let week = document.querySelector(`#currentWeek`);

    apiReq('my-tasks-checked', [{
      isChecked: elementRef.checked,
      id: elementRef.id,
      category: '',
      targets_category: targetsCategory,
      week: 1
    }])

  };

</script>

  <script type="text/javascript" src="/static/js/pop-msg.js"></script>

  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js" integrity="sha384-oBqDVmMz9ATKxIep9tiCxS/Z9fNfEXiDAYTujMAeBAsjFuCZSmKbSSUnQlmh/jp3" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.min.js" integrity="sha384-cuYeSxntonz0PPNlHhBs68uyIAVpIIOZZ5JqeqvYYIcEL727kskC66kF92t6Xl2V" crossorigin="anonymous"></script>


</body>

</html>